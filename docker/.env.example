# Copy to docker/.env and fill in real values. docker/.env is gitignored.
# `security setup` generates this automatically with a random ZAP_API_KEY if
# it doesn't already exist.

ZAP_API_KEY=
ZAP_API_PORT=8090
ZAP_VERSION=latest

MOBSF_PORT=8000
MOBSF_VERSION=latest

NUCLEI_VERSION=latest
